package DefaultCameraZoom

let DEFAULT_CAMERA_POS = 1000.

init
    for i = 0 to 10
        if players[i].isIngame()
            players[i].setCameraField(CAMERA_FIELD_ZOFFSET, DEFAULT_CAMERA_POS, 1)
